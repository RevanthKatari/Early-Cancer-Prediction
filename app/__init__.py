"""
UI package for the Early Cancer Prediction project.

The modules in this package provide:
- paths and display constants (`config`)
- lightweight preprocessing helpers (`preprocessing`)
- model loading and inference utilities (`predictors`)
- the Streamlit entry point (`main`)
"""
